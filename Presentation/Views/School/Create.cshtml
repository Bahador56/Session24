@model Models.SchoolModel
@{
	string Message = ViewBag.Message;
}
@if (!string.IsNullOrWhiteSpace(Message))
{
	<div class="alert alert-danger">
		<span> Insert Has an Error! </span>
		<br/>
		@Message
	</div>
}
<form action="\School\Create" method="post" asp-antiforgery="true">
	<div class="row col-md-12">
		<div class="form-group col-md-3">
			<label class="label">
				School Name
				<input type="text" class="form-control" name="@nameof(Model.Name)" value="@Model.Name" autocomplete="off" placeholder="Please enter name" />
			</label>
		</div>
		<div class="form-group col-md-3">
			<label class="label">
				Phone Number
				<input type="tel" class="form-control" name="@nameof(Model.PhoneNumber)" value="@Model.PhoneNumber" autocomplete="off" placeholder="+98..." />
			</label>
		</div>
		<div class="form-group col-md-3">
			<button type="submit" class="btn btn-sm btn-primary btn1234" style="margin-top:32px"> Save</button>
		</div>
	</div>
</form>